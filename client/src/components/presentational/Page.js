import React from 'react';
import Router from '../container/Router';
import NavBar from './NavBar';

const Page = () => (
    <main>
        <Router />
        <NavBar />
    </main>
);

export default Page;